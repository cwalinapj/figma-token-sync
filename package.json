{
  "name": "@origin-os/figma-token-sync",
  "version": "1.0.0",
  "description": "Sync Figma design tokens to Origin OS codebase",
  "main": "dist/sync.js",
  "types": "dist/sync.d.ts",
  "bin": {
    "figma-sync": "./bin/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/sync.ts",
    "sync": "ts-node src/sync.ts",
    "sync:watch": "ts-node src/sync.ts --watch",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "figma",
    "design-tokens",
    "css",
    "tailwind",
    "origin-os"
  ],
  "author": "Paul <cwalinapj>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.0",
    "chokidar": "^3.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
